{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcc",
            "args": [
                "-g",
                "hello.c",
                "main.c",
                "sum.c",
                // "*.cpp", //add all files at one time
                "-o",
                "test"
            ],
            "options": {
                "cwd": "${workspaceRoot}/code"
            }
        }
    ]
}